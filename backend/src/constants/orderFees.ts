/**
 * Order fee constants - single source of truth for platform and card fees.
 * Used by order controller so totals are always consistent.
 */
export const PLATFORM_FEE_PKR = 100;
export const CARD_FEE_PERCENT = 0.05;
